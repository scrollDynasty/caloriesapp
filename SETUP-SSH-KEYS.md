# Настройка SSH ключей для GitHub Actions

## 📋 Обзор

Для автоматического деплоя через GitHub Actions нужен SSH ключ, который будет использоваться для подключения к серверам:
- **Backend**: `scroll@api.yeb-ich.com`
- **Web**: `scroll@yeb-ich.com`

## 🔑 Шаг 1: Создание SSH ключа (если его нет)

### Вариант A: Создать новый ключ специально для GitHub Actions

```bash
# Создаём новый SSH ключ (без пароля для автоматизации)
ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/github_actions_deploy

# Или если ed25519 не поддерживается, используйте RSA:
ssh-keygen -t rsa -b 4096 -C "github-actions-deploy" -f ~/.ssh/github_actions_deploy
```

**Важно**: При запросе пароля нажмите Enter (оставьте пустым) для автоматизации.

### Вариант B: Использовать существующий ключ

Если у вас уже есть SSH ключ, который работает с сервером, используйте его.

## 📤 Шаг 2: Добавление публичного ключа на сервер

### Для Backend сервера (api.yeb-ich.com)

```bash
# Скопируйте публичный ключ на сервер
ssh-copy-id -i ~/.ssh/github_actions_deploy.pub scroll@api.yeb-ich.com

# Или вручную:
cat ~/.ssh/github_actions_deploy.pub | ssh scroll@api.yeb-ich.com "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

### Для Web сервера (yeb-ich.com)

```bash
# Скопируйте публичный ключ на сервер
ssh-copy-id -i ~/.ssh/github_actions_deploy.pub scroll@yeb-ich.com

# Или вручную:
cat ~/.ssh/github_actions_deploy.pub | ssh scroll@yeb-ich.com "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

### Проверка подключения

```bash
# Проверьте подключение к backend серверу
ssh -i ~/.ssh/github_actions_deploy scroll@api.yeb-ich.com

# Проверьте подключение к web серверу
ssh -i ~/.ssh/github_actions_deploy scroll@yeb-ich.com
```

Если подключение работает без запроса пароля — всё настроено правильно!

## 🔐 Шаг 3: Получение приватного ключа

```bash
# Покажите содержимое приватного ключа
cat ~/.ssh/github_actions_deploy

# Или если используете существующий ключ:
cat ~/.ssh/id_rsa
# или
cat ~/.ssh/id_ed25519
```

**Скопируйте весь вывод** (включая строки `-----BEGIN ... KEY-----` и `-----END ... KEY-----`)

## 🌐 Шаг 4: Добавление ключа в GitHub Secrets

1. **Откройте ваш репозиторий на GitHub**

2. **Перейдите в Settings**:
   - Нажмите на вкладку **Settings** в верхней части репозитория

3. **Откройте Secrets and variables → Actions**:
   - В левом меню найдите **Secrets and variables**
   - Нажмите **Actions**

4. **Добавьте новый секрет**:
   - Нажмите кнопку **New repository secret**
   - **Name**: `SSH_PRIVATE_KEY` (точно так, как написано)
   - **Secret**: Вставьте весь приватный ключ (который скопировали на шаге 3)
   - Нажмите **Add secret**

## ✅ Шаг 5: Проверка настройки

После добавления ключа:

1. Сделайте небольшое изменение в коде
2. Закоммитьте и запушьте в `master`:
   ```bash
   git add .
   git commit -m "Test deployment"
   git push origin master
   ```
3. Перейдите в **Actions** на GitHub
4. Проверьте, что workflow запустился и деплой прошёл успешно

## 🔍 Устранение проблем

### Проблема: "Permission denied (publickey)"

**Решение**:
- Убедитесь, что публичный ключ добавлен на сервер
- Проверьте права доступа: `chmod 600 ~/.ssh/authorized_keys` на сервере
- Проверьте, что пользователь существует на сервере

### Проблема: "Host key verification failed"

**Решение**:
- Это нормально для первого подключения
- GitHub Actions автоматически добавляет сервер в known_hosts

### Проблема: Деплой не запускается

**Решение**:
- Проверьте, что вы пушите в правильную ветку (`master` для production)
- Проверьте, что файлы изменены в нужных директориях (`backend/**` или `web/**`)
- Проверьте логи в GitHub Actions

## 📝 Важные замечания

1. **Безопасность**: Никогда не коммитьте приватные ключи в репозиторий!
2. **Доступ**: Убедитесь, что ключ имеет доступ только к нужным серверам
3. **Ротация**: Периодически меняйте ключи для безопасности

## 🎯 Быстрая команда для копирования ключа

Если у вас уже есть рабочий SSH ключ, используйте эту команду для быстрой настройки:

```bash
# Показать приватный ключ для копирования
echo "=== Приватный ключ (скопируйте всё) ==="
cat ~/.ssh/id_rsa || cat ~/.ssh/id_ed25519 || cat ~/.ssh/github_actions_deploy
```

