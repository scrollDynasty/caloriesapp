# 🚀 Инструкция по масштабированию приложения

## Что было создано

Я подготовил полную инфраструктуру для масштабирования вашего приложения до 10,000+ пользователей:

### ✅ Docker
- `Dockerfile` - образ для бэкенда
- `docker-compose.yml` - для локальной разработки с Redis и MySQL

### ✅ Redis интеграция
- Сервис кэширования (`app/services/cache.py`)
- Автоматическое кэширование данных пользователей
- Инвалидация кэша при изменениях

### ✅ Kubernetes
- Все манифесты для production деплоя
- Автомасштабирование (HPA) - от 3 до 10 подов
- Health checks для всех сервисов
- Persistent volumes для Redis

### ✅ Скрипты
- `deploy.sh` - автоматический деплой
- `build-and-push.sh` - сборка и загрузка образа

## 📋 Быстрый старт

### 1. Локальное тестирование

```bash
cd backend

# Создайте .env файл
cp env.prod.sample .env
# Отредактируйте .env

# Запустите все сервисы
docker-compose up -d

# Проверьте работу
curl http://localhost:8000/health
```

### 2. Сборка образа

```bash
cd backend

# Установите ваш Docker registry
export DOCKER_REGISTRY="your-registry.com"

# Соберите и загрузите образ
chmod +x scripts/build-and-push.sh
./scripts/build-and-push.sh v1.0.0
```

### 3. Подготовка Kubernetes

```bash
cd k8s

# Создайте namespace
kubectl apply -f namespace.yaml

# Создайте secrets (ВАЖНО: замените на реальные значения!)
kubectl create secret generic app-secrets \
  --from-env-file=../backend/.env \
  -n caloriesapp
```

### 4. Обновление конфигурации

Отредактируйте `k8s/backend/deployment.yaml`:
- Замените `your-registry/caloriesapp-backend:latest` на ваш registry

### 5. Деплой

```bash
cd k8s
chmod +x deploy.sh
./deploy.sh production
```

## 🔧 Настройка для 10,000 пользователей

### Ресурсы

**Минимальные требования:**
- 3 worker nodes
- Каждый node: 4 CPU, 8GB RAM
- Storage: 50GB для БД, 10GB для Redis

**Рекомендуемые:**
- 5 worker nodes
- Каждый node: 8 CPU, 16GB RAM
- Управляемая MySQL (Yandex Cloud Managed MySQL)
- Redis: 2GB памяти

### Масштабирование

Автоматическое масштабирование уже настроено:
- **Минимум:** 3 пода
- **Максимум:** 10 подов
- **По CPU:** 70% использования
- **По памяти:** 80% использования

Для ручного изменения:
```bash
kubectl scale deployment backend-api --replicas=5 -n caloriesapp
```

## 📊 Мониторинг

```bash
# Статус подов
kubectl get pods -n caloriesapp

# Логи
kubectl logs -f deployment/backend-api -n caloriesapp

# Использование ресурсов
kubectl top pods -n caloriesapp

# Статус HPA
kubectl get hpa -n caloriesapp
```

## 🔄 Обновление приложения

```bash
# 1. Соберите новый образ
cd backend
./scripts/build-and-push.sh v1.1.0

# 2. Обновите deployment
kubectl set image deployment/backend-api \
  backend=your-registry/caloriesapp-backend:v1.1.0 \
  -n caloriesapp

# 3. Проверьте статус
kubectl rollout status deployment/backend-api -n caloriesapp
```

## 🎯 Что оптимизировано

1. **Кэширование:**
   - Данные пользователей кэшируются на 5 минут (текущий день)
   - Прошлые дни кэшируются на 1 час
   - Автоматическая инвалидация при изменениях

2. **База данных:**
   - Connection pooling настроен
   - Рекомендуется использовать управляемую БД

3. **Производительность:**
   - Автомасштабирование по нагрузке
   - Health checks для быстрого восстановления
   - Session affinity для стабильности

## ⚠️ Важные моменты

1. **Secrets:**
   - НИКОГДА не коммитьте `.env` или `secrets.yaml` с реальными значениями
   - Используйте Kubernetes Secrets
   - Для production рассмотрите Vault

2. **Образы:**
   - Используйте приватный registry
   - Используйте конкретные теги, не `latest` в production

3. **Безопасность:**
   - Настройте Network Policies
   - Используйте TLS везде
   - Ограничьте доступ к admin панели

## 🆘 Решение проблем

### Поды не запускаются

```bash
# Проверьте логи
kubectl logs <pod-name> -n caloriesapp

# Проверьте события
kubectl get events -n caloriesapp --sort-by='.lastTimestamp'
```

### Redis недоступен

```bash
# Проверьте статус
kubectl get pods -l app=redis -n caloriesapp

# Проверьте логи
kubectl logs deployment/redis -n caloriesapp
```

### Высокая нагрузка

1. Проверьте количество подов: `kubectl get pods -n caloriesapp`
2. Увеличьте количество реплик: `kubectl scale deployment backend-api --replicas=5 -n caloriesapp`
3. Проверьте использование ресурсов: `kubectl top pods -n caloriesapp`

## 📞 Следующие шаги

1. ✅ Настройте мониторинг (Prometheus + Grafana)
2. ✅ Настройте логирование (Loki + Grafana)
3. ✅ Настройте алерты
4. ✅ Настройте автоматические бэкапы БД
5. ✅ Настройте CI/CD pipeline

## 📚 Полезные команды

```bash
# Перезапуск
kubectl rollout restart deployment/backend-api -n caloriesapp

# Откат
kubectl rollout undo deployment/backend-api -n caloriesapp

# Порт-форвардинг
kubectl port-forward svc/backend-service 8000:80 -n caloriesapp
```

---

**Готово!** Ваше приложение готово к масштабированию. Все файлы созданы и настроены. Следуйте инструкциям выше для деплоя.

