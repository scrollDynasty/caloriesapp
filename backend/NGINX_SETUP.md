# 🔧 Настройка Nginx для балансировки нагрузки

## Текущая конфигурация

Запущено **4 PM2 процесса** на портах:
- `8000` - caloriesapp-backend-1
- `8001` - caloriesapp-backend-2  
- `8002` - caloriesapp-backend-3
- `8003` - caloriesapp-backend-4

## Применение конфигурации Nginx

```bash
# 1. Скопируйте конфигурацию
sudo cp nginx/api.yeb-ich.com.conf /etc/nginx/sites-available/api.yeb-ich.com.conf

# 2. Проверьте конфигурацию
sudo nginx -t

# 3. Если всё ОК - перезагрузите nginx
sudo systemctl reload nginx
```

## Преимущества этой конфигурации

✅ **Высокая производительность:**
- 4 PM2 процесса × 2 workers = **8 worker процессов**
- Каждый процесс обрабатывает до 50 одновременных запросов
- Итого: **до 400 одновременных запросов**

✅ **Отказоустойчивость:**
- Если один процесс упадет, остальные продолжают работать
- Nginx автоматически исключит неработающий процесс из балансировки

✅ **Равномерное распределение:**
- Round-robin алгоритм распределяет запросы равномерно
- Каждый процесс получает ~25% нагрузки

## Мониторинг

```bash
# Статус всех процессов
pm2 status

# Логи конкретного процесса
pm2 logs caloriesapp-backend-1
pm2 logs caloriesapp-backend-2
pm2 logs caloriesapp-backend-3
pm2 logs caloriesapp-backend-4

# Мониторинг в реальном времени
pm2 monit

# Перезапуск всех процессов
pm2 restart ecosystem.config.js
```

## Проверка работы

```bash
# Проверка портов
curl http://localhost:8000/health
curl http://localhost:8001/health
curl http://localhost:8002/health
curl http://localhost:8003/health

# Проверка через nginx (балансировка)
curl https://api.yeb-ich.com/health
```

